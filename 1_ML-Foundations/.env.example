# Local lab configuration only. Do not put real credentials here.
LAB_ENV=local
MODEL_VERSION=fraud-risk-local-v1
API_HOST=127.0.0.1
API_PORT=8000

# Optional future AWS documentation/demo values.
# Leave empty unless a future task explicitly asks for AWS integration.
AWS_REGION=
AWS_S3_BUCKET=
AWS_PROFILE=

